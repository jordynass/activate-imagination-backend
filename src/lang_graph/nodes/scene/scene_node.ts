import { GraphAnnotation } from "../../entities/state";

export function sceneNode(state: typeof GraphAnnotation.State): typeof GraphAnnotation.State {
  console.log('TODO: Implement SceneNode');
  return state;
}